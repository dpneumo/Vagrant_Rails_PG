# Vagrant linux vm, windows host
#
# .gitattributes
#
# Reference: https://help.github.com/articles/dealing-with-line-endings/

# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Shell scripts intended to run on the linux based VM
# should always have LF line endings on checkuot.
*.sh text eol=LF
